#ifndef ADMIN_DASHBOARD_H
#define ADMIN_DASHBOARD_H

// Displays the Admin options (Process Requests, Manual Add, Logout)
// Returns the user's choice as an integer.
int Show_Admin_Dashboard(char *admin_name);

#endif