#ifndef STUDENT_DASHBOARD_H
#define STUDENT_DASHBOARD_H

// Displays the Student options (Register, Logout)
// Returns the user's choice as an integer.
int Show_Student_Dashboard(char *student_name);

#endif